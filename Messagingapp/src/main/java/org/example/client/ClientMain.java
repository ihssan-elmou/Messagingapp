package org.example.client;

import org.example.client.ui.LoginView;
import javafx.application.Application;

public class ClientMain {
    public static void main(String[] args) {
        Application.launch(LoginView.class, args);
    }
}